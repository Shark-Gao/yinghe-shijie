# AGENTS.md - 硬核视界 AI 工作规范

## 项目目标

维护「硬核视界」面向多平台分发的硬核知识短视频项目。B 站不再是唯一发布目标；围绕同一优质海外源视频，按平台特点制作并分发不同版本。

## AI 助手职责

- 推荐符合定位的 YouTube 视频
- 维护视频库和制作记录
- 优化标题、封面文案
- 遵守项目规则

## 项目技能位置

- 所有为「硬核视界」新增或维护的项目专属技能，必须存放在本项目的 `skills/` 目录下。
- 不将项目专属技能安装到用户级 Codex 技能目录；通用系统技能不受此限制。

## 技能文档语言规则

- 项目技能的 `SKILL.md`、`agents/openai.yaml`、`references/` 文档及后续新增的说明性文件默认全部使用中文。
- 后续修改技能时也必须使用中文；技能名、路径、命令、脚本名、JSON/YAML 字段名、平台键名、声音名和其他机器接口标识按实际要求保留，不翻译。
- 脚本代码、命令参数和程序要求的英文数据可以保留；自然语言说明、注释、默认提示和用户可见文案统一使用中文。

## 当前制作流程 V1.1

推荐视频 → Telegram 下载 MP4 → 剪映生成中英字幕 → 按平台制作版本 → 多平台发布 → 记录数据并复盘

## 短视频生成偏好

- 用户提到“生成短视频”时，默认使用项目内的 `yinghe-short-video` 技能：以原始视频和字幕为输入，产出平台适配的中文短视频。
- 不使用 `local-text-to-video` 技能，也不采用“静态图片轻微动效”的方式替代短视频制作。

## 默认包装交付

- 进入军事向或女性向的短视频、长视频制作流程后，默认都必须生成三种封面：横版 `16:9`、`4:3` 和 `3:4` 竖版；必须根据内容类型选择对应视觉系统。封面文字后期叠加，不依赖生成模型直接写中文。
- 长视频和短视频的封面都是制作交付物，不因用户没有在最后一句单独重复“生成封面”而省略；只有用户明确说“不要封面”或“跳过封面”时才允许跳过，并在计划和验收记录中写明原因。
- 短视频默认只交付一个平台版本的标题和发布简介；用户未指定平台时，默认按抖音版本生成。简介必须是可直接粘贴的完整文案，结尾默认附 3—4 个与视频内容相关的话题标签，最多不超过 4 个，并与口播、封面承诺同一个结果。用户明确要求“通用标题、通用简介、话题”时，改为交付不绑定平台的通用三件套。
- 用户未再次提醒时，也必须按上述默认包装完整交付；封面、发布包装和三种尺寸文件缺失时，禁止把 MP4 标记为正式完成。

## 内容类型与封面分流

- 项目允许军事向和女性向两种制作路线；账号或本次任务的方向以用户明确指定为准，不能把两种内容混入同一条包装。
- 军事向封面使用深蓝黑、钢铁灰、青蓝或克制橙色，高对比、强工程感；主体优先为装备、地图、剖面、雷达轨迹、舰船、战机、导弹或军工场景，标题突出冲突、规模、成本、瓶颈或结果。
- 女性向封面使用深海军蓝、薄荷绿/青绿色和珊瑚色，采用温暖非写实编辑插画；主体围绕稳定发挥、情绪、关系、亲子或女性成长，标题突出具体问题和可验证的行动收益。
- 生成封面前必须确定 `cover_style` 为 `military` 或 `female`；方向不明确时先确认，不使用折中的混合风格。

## 多平台分发原则

- 采用“一源多剪、多平台分发”：同一源视频可根据不同平台的节奏、开头、标题和封面制作不同版本。
- 抖音、快手、小红书优先制作 30–90 秒、开头强钩子、信息密度高的版本；标题突出冲突、规模、成本、竞争或结果。
- B 站作为较完整内容、系列化内容和账号品牌沉淀的阵地，不再用 B 站的推荐逻辑决定全部选题和剪辑方式。
- 小红书强化“冷知识 + 可收藏”的表达；抖音、快手强化前几秒吸引力和视觉奇观。
- 每次发布后记录各平台的播放、完播、互动等数据，据此调整选题、时长、包装与优先发布平台。

## 男性向内容定位 V3

- 男性向视频默认使用 Microsoft Edge TTS 声音 `zh-CN-YunyangNeural`，语速默认 `+10%`；除非用户明确指定其他声音，不使用 CosyVoice 男声。
- 短视频默认不合并背景音乐，也不填写 `background_music`；只有用户明确要求或平台版本确有需要时，才启用 BGM，并单独记录音量。
- 男性向主版本以故事完整为优先，不把 30–90 秒当作硬性时长；需要完整解释时可以超过 90 秒，不通过删掉必要证据、硬切结尾或无理由加速来凑时长。
- 核心定位：只从军事武器、武器系统、作战机制和技术细节，看懂武器如何作战及其现实影响。
- 男性向内容范围仅限军事武器及直接相关内容：单件武器与平台、武器系统、探测/制导/防御/电子战等直接服务于武器使用的技术，以及武器的作战机制、限制、成本和军工使用场景。
- 地图、国际局势和战争背景只能作为解释武器的上下文；不得把纯地缘政治、国际新闻或立场评论单独作为男性向选题。
- 每条内容先确定一个普通人问题、一个明确答案和一种看完后的情绪变化，再写标题和解说。
- 开头优先在 2 秒内给出冲突或结果，5—10 秒内交付核心答案；不要从背景介绍或频道自我介绍开始。
- 学习同行时复制开头、节奏、系列和评论设计，不复制文案、素材或未经核实的观点。
- 数据判断优先看 2 秒、5 秒、10 秒留存、完播、收藏、转发、评论质量和播放转粉，不单独追逐粉丝总数。

## 选片原则

- 优先少真人或无真人出镜
- 优先 3D 武器动画、装备剖面、军事工程可视化、纪录片、作战地图动画
- 不重复推荐同一个源视频；同一武器类别、同一系列或相近方向可以重复，只要源视频 URL 不同
- 保持主题系列化，方向多样性只作为候选质量接近时的排序参考，不作为硬性淘汰条件
- 军工装备为高优先级题材；在候选质量相当时，优先航母、潜艇、战机、导弹防御、无人装备等少真人、强工程可视化内容。

## 国内化选题思路

- 国内化表达是包装加分项，不是推荐硬门槛。先按题材质量、画面、信息密度和可解释性选片；没有中国关联的优秀军事装备或工程视频也可以推荐。
- 如有中国关联，再判断能否包装成国内观众熟悉的热门选题；不要为了制造关联，强行把普通装备题材改写成敏感地缘政治话题。
- 优先选择能对应国内同类军事科普内容的素材：航母、潜艇、战机、坦克、火炮、导弹、防空反导、无人机、雷达声呐、电子战、弹药和武器系统。
- 军工生产、地图、战争史和国际局势只有在服务于具体武器解释时才纳入；普通工程、AI、芯片、能源、汽车、地理冷知识和商业科技不再作为男性向选题。
- 降低纯冷门原理题材的优先级，除非它能转化成明确的军事武器问题，例如“导弹为什么能锁定目标”“航母内部如何保障舰载机起降”“防空系统为什么会漏掉目标”。
- 中文标题要优先突出冲突、结果、规模、成本、稀缺性或现实用途，而不是直译英文标题。
